name: My First Workflow

on: push
# on: [push, fork]

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
        - name: Welcome Message
          run: echo "my first github action job"

        - name: list file
          run: ls

        - name: read files
          run : cat README.md 
